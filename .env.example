# Configuración del Backend
# URL base del servidor backend para las peticiones API
VITE_BACKEND_URL=http://localhost:3000/api

# Configuración de Desarrollo (opcional)
# PORT=5173
# HOST=localhost

# Configuración de Producción (opcional)
# Para builds de producción, configurar la URL del backend desplegado
# VITE_BACKEND_URL=https://api.tu-dominio.com/api
